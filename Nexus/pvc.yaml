---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: nexus
  name: nexus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
